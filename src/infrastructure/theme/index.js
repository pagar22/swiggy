import { colors } from './colors';
import { fonts, fontWeight, fontSizes } from './fonts';
import { sizes } from './sizes';
import { space, lineHeight } from './spacing';

export const theme = {
    colors,
    fonts,
    fontWeight,
    fontSize,
    sizes,
    space,
    lineHeight,
}